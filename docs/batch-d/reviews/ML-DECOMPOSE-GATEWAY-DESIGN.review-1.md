correction

Review of ML-DECOMPOSE-GATEWAY-DESIGN, candidate 9e4a71090c8a8a9e82f85a49caf959bf9b9434a8 (base e74fb88), reviewer agent:claude-fable-5-1/review-ML-DECOMPOSE-GATEWAY-DESIGN, worktree /private/tmp/review-ML-DECOMPOSE-GATEWAY-DESIGN. Scope check: the candidate changes exactly one path, docs/design/DECOMPOSE-GATEWAY.md (+216), inside the packet scope. `elixir bin/check_docs.exs`: 0 broken links.

The call graph, module assignment, invariant list, external-caller table, test counts and pin count are all verified against the code and hold. Two parts of the note would mislead an implementer or the rebinding lead as written, so the verdict is correction, not approved. Fixes are small and confined to sections 3 and 4.

## Corrections (required)

1. **section 3, FR-08A rebind steps: "each with a distinct placeholder hash" (one per tuple) breaks the rebind.** bin/rebind_fr08a.exs:30-41 builds `[{old_sha, new_sha}, {old_md5, new_md5}]` per pinned entry from `identity().exercised_api` and applies them with `String.replace/3` over the whole file (:44-49). If one tuple's sha256 slot and beam_md5 slot carry the same placeholder, the first pair rewrites both slots to the sha256 and the second pair finds nothing, so the beam_md5 stays wrong and `implementation_binding` stays "mismatch" after the rebind (lib/foundry/repair/fr08a_protected_boundary.ex:636-641). Fix: state "two distinct placeholders per tuple, one in the sha256 slot and one in the beam_md5 slot, six in total, none a substring of any other text in the provider or test". Also state the two steps the script header (bin/rebind_fr08a.exs:1-11) requires and the note omits: (a) commit the hand edits (three tuples, `== 10` -> `== 13`) before running, because the script raises on a dirty tree (:16-17); (b) regenerate docs/fr-08/fr08a-protected-report.txt in a fresh VM with the command the script prints (:52-56) and commit it with the provider and test, because the test asserts byte-equality with that file (test/foundry/repair/fr08a_protected_boundary_test.exs:41). The count 10 -> 13 itself is correct: 10 tuples at fr08a_protected_boundary.ex:18-51, `length(...) == 10` at the test's line 12; only protected_primitives.ex and gateway.ex have per-file source lines in the test (:45-49), so the new modules need no test lines.

2. **section 4, move check: a source-AST comparison cannot see what a move changes.** `Code.string_to_quoted!/2` yields unexpanded AST: aliases, module attributes and `__MODULE__` are resolved at compile time, so two files whose clauses compare AST-equal can compile to different code. The concrete trap in this file: gateway.ex:13-25 aliases `Foundry.DurableStore.Kernel`, which shadows Elixir's `Kernel`; `normalize_candidate/1` at :1475 calls `Kernel.normalize_bundle/1`. A DomainCommit that forgets the alias has an AST-identical clause that means something else (here compile catches it only because Elixir.Kernel has no `normalize_bundle/1`; the method must not rely on that). Step 3's rewrite of `DomainCommit.f(` to `f(` is also a hand-written normaliser that has to be trusted. Stronger check, no extra tooling: compare compiled definitions. For each of the four post-commit beams and the base Gateway beam (built in a scratch worktree at the base), read `:beam_lib.chunks(beam, [:debug_info])`, call `backend.debug_info(:elixir_v1, module, data, [])` and take `definitions` (`{{name, arity}, kind, meta, clauses}` with every alias expanded to a full module atom, attributes inlined and `__MODULE__` substituted). Normalise by stripping meta, mapping `defp` to `def` and mapping the module atoms {Gateway, DomainCommit, AtomicBundle, Maintenance} to one placeholder; then assert the multiset of post-commit definitions equals the base multiset plus the declared `defdelegate`s. That proves the compiler saw the same expanded body for every moved function modulo the module rename, and it subsumes steps 1-3. Keep the grep counts and the `--color-moved` read as they are, but say that `mix format` will reflow lines that grow a `DomainCommit.`/`AtomicBundle.`/`Maintenance.` prefix past the line limit, so reflowed lines are expected and the definition check, not the diff, is the authority.

## Minor (fix in the same pass)

3. section 4, T1: "the three copies of `SELECT coalesce(max(seq), 0) FROM events` in D" miscounts. D has two (gateway.ex:2103, :2170); the third in gateway.ex is `current_seq/1` at :1946, which is region B and stays in DomainCommit. After M1 Maintenance holds three (the two moved plus its own `last_sequence/1`, maintenance.ex:42-47); say so, so T1 does not reach into DomainCommit.
4. section 1, region B "Calls into" omits TransitionPlan: `normalize_candidate/1` calls `TransitionPlan.slot_event_types/0` at gateway.ex:1477. "Aliases follow the calls" covers it in M2, but the table is what the implementer reads.
5. section 1: "Each `handle_call` has a `%{mode: :recovery}` clause" is untrue for `:status` (gateway.ex:127-129), which answers in both modes by design. Harmless; reword.
6. section 4, T2: docs/DURABLE-STORE.md contains no `gateway.ex` path (it names the module and the API, lines 9, 61, 105-124, all still true after the split), so that row is likely a no-op; docs/REPAIR-PLAN.md:240 does cite `gateway.ex` (2,377) and is a live document. Either add it or state it is a dated figure left alone.

## Verified, no change needed

- Regions partition every definition: A 1-489, 2056-2067, 2077-2096, 2435-2448; B 491-551, 1471-2054, 2069-2075, 2450-2455; C 553-1469; D 2098-2433. No function is assigned twice or to none.
- Call matrix (built from every `defp`/`def` name against its call sites): C -> B is exactly the 9 named functions (`prepare_command` :1226, `normalize_candidate` :1360, `check_expected_revisions` :960, `commit_accepted_bundle` :962, `commit_rejected_command` :1033/:1083, `inject` :669-:1111, `validate_actor` :571, `projection_key` :1466, `encode_key` :1469). A -> B is `do_transact` :193, `do_verified_transact` :220, `fetch_command` :239. A -> C is `do_atomic_bundle` :295. A -> D is the 7 listed (:139, :146, :175, :184, :372, :385, :386/:402/:418). B -> C, D -> B, D -> C: none. `get/3` (:2450) is used only at :1956-:1993 (B); `semantic_rejection?/1` (:2069) only at :1552 (B). 20 functions widen, as the note says.
- No seam-crossing process or module identity: `__MODULE__` only at :52 (A); `self()` at :141 (A) and :2226 inside the spawned controller, whose messages to the Gateway carry no module name; `@capacity_probe_timeout_ms` used at :463 and :2445 (A); `@domain_read_namespaces` at :1417/:1425 (C). No Logger, telemetry, registered names, imports or `Process` dictionary use. No test matches on a module frame or module-tagged error; tests use `{Gateway, opts}` only as a child spec; test/support fixtures call only the public API plus `module_info(:compile)` for the needle line, which stays (`def command` :68).
- Invariant literals match: transactions at :666, :1081, :1510 (3, none nested; `commit_accepted_atomic_bundle` runs inside :666's fun); halts 71 :2052, 72 :1539, 74 :2186; digest domains :592, :1499; `"input:"` :1675/:1685; `atomic-v2/` :944; `'domain_v1'` :1664, `'bundle_v2'` :1180; `Owner.acquire` :31/:441, release :38/:122/:475/:481; `Authority.read` inside transactions :993/:1060/:1110/:1603/:1641; 58 distinct `{:error, :atom` prefixes. Rule 3 ignores comments (architecture_boundary_test.exs:73-76), so the `pm` comment at :1408 can move.
- External-caller table matches `grep -rn 'Gateway\.' lib` exactly, including protected_primitives.ex:6048 (fully qualified, so the `defdelegate` keeps it edit-free) and plan.ex:34 (comment). No lib caller of `transact_verified`. Test counts spot-checked: 35 files, `status` 101, `transact` 64, `transact_verified` in 8 files.
- Q2 is feasible as posed: `atomic_domain_request/1` (:1348-1358) calls only `atomic_carrier_name/1` (:1345-1346), so the 14 lines move without dragging B or C.
- Sequencing: ML-DEAD-ROUTES is a real batch C1 ticket (docs/batch-d/DOGFOOD-LOG.md:30-31) covering both the verifier route and Observations, and the note handles both, including gateway_source.ex. Landing it first is right. No protected_primitives design note exists at this base (docs/design/ has none), so section 6's S1-S5 are the coordination surface to re-check when that note lands; say so in one line.

## Commands run (all in the review worktree)

- `git diff --stat e74fb88..HEAD`: 1 file, docs/design/DECOMPOSE-GATEWAY.md +216.
- `elixir bin/check_docs.exs`: 0 broken link(s).
- grep listings of every def/defp/alias/attribute in gateway.ex; per-function call-site matrix over all 130 names; grep for `__MODULE__|self()|spawn|Logger|telemetry|Process.` ; grep counts for `Database.transaction` (3), `System.halt` (3), digest literals, `{:error, :atom` prefixes (58 distinct).
- `grep -rn 'Gateway\.' lib` (excluding gateway.ex): 36 call sites in 7 files, matching the note's table.
- `grep -rlE 'Gateway\.' test | wc -l`: 35; `Gateway.status(` 101; `Gateway.transact(` 64; files with `transact_verified(`: 8.
- Read in full: bin/rebind_fr08a.exs, lib/foundry/durable_store/maintenance.ex, test/support/fr08a_identity_negative_fixture.exs, fr08a_protected_boundary.ex:1-80 and :620-648, fr08a_protected_boundary_test.exs:1-62, gateway.ex regions :1-26, :130-170, :664-724, :1337-1372, :1474-1480, :2056-2130, :2224-2245, :2352-2456.
- No tests run: the candidate changes documentation only.
