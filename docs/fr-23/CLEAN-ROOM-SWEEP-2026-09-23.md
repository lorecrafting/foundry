# Clean-room sweep — 2026-09-23

**Type:** read-only audit by a fresh Fable agent. Nothing was edited, deleted, moved, committed or
run beyond `git`, `gh`, `grep`, `find`, `du`, `ls`, `cat`, `diff`. No test, gate or Quint run.
**Baseline:** `25f7174` (`integ/lane-batch-1`). While this sweep ran, PR #2 merged and
`origin/main` moved to the same commit (`git ls-remote origin main` → `25f7174…`; `gh pr list
--state all` → #1 and #2 `MERGED`). Every path and line below is at that tree.
**Builds on:** [dead surface](../archive/DEAD-SURFACE-INVENTORY-2026-09-22.md), [doc retirement](../archive/DOC-RETIREMENT-INVENTORY-2026-09-22.md),
[split proposal](FR-23-SPLIT-PROPOSAL-2026-09-22.md), [worktree inventory](WORKTREE-INVENTORY-2026-09-23.md),
[bin health](../archive/BIN-SCRIPT-HEALTH-2026-09-22.md). Where a verdict differs from theirs it is because the
operator's 2026-09-23 stance (not in production; clean-room rebuild; fresh stores) removes the
"behaviour-preserving, persisted-format-frozen" constraint those inventories were written under.
**Forward path used as the yardstick:** the manual lane (`ManualLane.*` → `DurableStore.Gateway`/
`ProtectedPrimitives` → `Workflow.Kernel`), per [DOGFOOD-READINESS](../DOGFOOD-READINESS-2026-09-23.md) §4,
[LANE-RUNBOOK](../batch-d/LANE-RUNBOOK.md) and [DOGFOOD-LOG](../batch-d/DOGFOOD-LOG.md).

Marks: **(inferred)** = not verified by a command; everything else was run or read.

> **Operator check, 2026-09-23 (after the sweep).** The lane store's `authority.sqlite3.owner.unclean`
> marker belongs to the running lane daemon, not a fenced store: `lane status` reports `mode: ready`.
> No `pramana_foundry` node runs (Q11); the other BEAM processes are Pramāṇa's `mix pramana.mcp.stdio`.
> `local/` mtimes predate the data's move into this checkout.

## Headline numbers

| Measure | Value | Command |
|---|---|---|
| Tracked files | 588: docs 249, test 141, lib 140, bin 18, ci 9, spec 8, roles 5, config 5, rel 3, .github 2 | `git ls-files \| cut -d/ -f1 \| sort \| uniq -c` |
| `lib/` lines | 45,479 in 140 files; `protected_primitives.ex` 8,191, `gateway.ex` 2,475, `coordinator.ex` 1,641, `authority.ex` 1,423 | `git ls-files lib \| xargs wc -l` |
| Legacy-only `lib/` lines (the subsystems in §1 marked delete) | **18,828 of 45,479 (41%)** | `git ls-files lib \| grep -E '<delete-list regex>' \| xargs wc -l` (pattern in §1 footnote) |
| Legacy-only `test/` lines | **12,015** in ~59 files, of 43,472 | same, over the §1 test list |
| Lines containing `pramana` (case-insensitive) | **3,533 in 499 files**: docs 1,998, test 804, lib 539, bin 78, ci 50, README 22, roles 17, config 8, spec 7, mix.exs 5 | `git grep -i -c pramana integ/lane-batch-1 -- .` |
| `PRAMANA_*` env names | `PRAMANA_RUNTIME_ROOT` 36, `PRAMANA_RELEASE` 26, `PRAMANA_RUNTIME_ROOT_FRESH` 25, `PRAMANA_OPERATOR_RUNTIME_ROOT` 15, `PRAMANA_STARTUP_MODE` 9, `PRAMANA_FOUNDRY_RUNTIME_DIR` 4 | `git grep -h -o -E 'PRAMANA_[A-Z_]+' … \| sort \| uniq -c` |
| Pramāṇa GitHub links pinned to `e1e4b3bf` | 25 links in 17 files | `git grep -c 'lorecrafting/pramana/blob/e1e4b3bf' -- .` |
| `docs/` | 213 `.md` + 36 other; 57,849 lines; `fr-08/` alone 77 files / 13,391 lines; `IMPLEMENTATION-LOG.md` 5,736 lines, 95 headings | `git ls-files docs \| xargs wc -l` |
| Docs cited by no other file | 9 (listed in §3) | per-file `git grep -l -F <basename>` minus self |
| Gate | PR #2 Linux CI `pass` 6m47s; DOGFOOD-LOG F3: 1,232 tests macOS / 1,229 Linux | `gh pr checks 2`, `gh run list` |

## Summary: the ten highest-value actions, ranked

| # | Action | Why first | Size |
|---|---|---|---|
| 1 | **Delete the legacy daemon stack before renaming it** (§1 rows marked delete: Coordinator/Tick/State, AgentServer, Herdr, launch Effects, JSONL persistence, Board/Status/Telemetry, Improver/HardeningPM/PM/Scheduler, Assignments/Reviews/Integration, RuntimeOwner/Lease/Fence/Cleanup, legacy CLI commands, `roles/`) | 41% of `lib/`, ~28% of `test/`, and most of the 539 `lib/` `pramana` hits vanish; FR-23b then renames half the surface. Nothing on the lane path calls any of it (`test/pramana_foundry/manual_lane/non_launch_test.exs:26` proves the closure) | L |
| 2 | **Reduce `Application` to lane-only** and drop `PRAMANA_STARTUP_MODE`, `COORDINATOR_TICK`, herdr/poll/max_* config (`application.ex:14-19,64-96`; `config/config.exs:11-19`) | The lane is the only supported daemon (`application.ex:99-100`) | S (inside #1) |
| 3 | **Decide the FR-15aA pin set** (operator question Q2): its 12 `source_route` sha256 pins are exactly the files #1 deletes (`ci/validate_fr15aa.exs:206-246`) | #1 cannot land without either rebinding or archiving it | S |
| 4 | **Rename (FR-23b)** after #1: namespace, app, `bin/pramana`, env, domain tags, `mix.exs` release name, `.gitignore /pramana_foundry`, `ci.ex` schema/tmp names | One mechanical commit + one `bin/rebind_fr08a.exs` | L |
| 5 | **Archive dated evidence** into `docs/archive/` (whole dirs `fr-01…fr-07, fr-09, fr-15a, fr-18a, fr-19a, fr-21, audit-2026-09-12`, the five top-level audits, `IMPLEMENTATION-LOG.md`, the four 2026-09-22 inventories) and retire the four mechanism docs whose mechanism #1 removes (`EVENT_SOURCING`, `MIGRATION`, `MIGRATION-TICKETS`, `ASSESSOR`) | 8 doc files are read by tests/tools and must not move without a rebind (§3) | M |
| 6 | **Delete the Python-era and research leaves**: `Assessor.*` (1,727 lines, 13 files, 0 lib callers outside itself), `Preparation`, `Parity`, `Projections.*`, `Schema`, `test/fixtures/{python,projections,telemetry}`, `config/schemas/` | Disjoint from #1; each is a one-agent ticket | S×3 |
| 7 | **Retire H0 and the legacy import** (`LegacyImport`, `LegacyLine`, `DurableStore.Kernel` behaviour, `H0AcceptedFR07Boundary`, its report, the two `pramana/<sha>` tags, CI `fetch-depth: 0`) with one FR-08A rebind | Fresh stores (DOGFOOD-LOG Q1) leave nothing to import; H0 is a superseded checkpoint that alone forces full-history checkouts in CI | M |
| 8 | **Repo hygiene, operator-run**: `rm -r local/ handoffs/ ci-artifacts/`; remove the 3 `.claude/worktrees` (all cherry-equivalent to main); delete `chore/split-cleanup` and `integ/lane-batch-1` local+remote; decide the 12 `archive/2026-09-20/*` tags; delete `.github/workflows/fr19a-sync-eio.yml` (gated on a branch this remote does not have) | Minutes; no agent needed | S |
| 9 | **Land the six DOGFOOD-LOG frictions as lane tickets** (F1 quiet RPC stdout, F4 reviewer worktree, F5 notes archive, F6 `lane integrate`) plus a direct `GitEvidence` test (it has none: `git grep -l GitEvidence -- test` → 0 files; only `manual_lane/cli_test.exs:235` hits the refusal) | The lane is the product now | S×4 |
| 10 | **Fix the operator's Claude memory pointers** (§4.6: 11 of 21 files carry pre-split paths or the legacy supervisor) | Every new session reads them | S, operator |

## 1. Code

Caller counts come from a per-module scan (`git grep -l -P '\bPramanaFoundry\.X\b|alias PramanaFoundry\.Parent\.\{[^}]*\bLast\b' -- lib test bin ci docs`, excluding the defining file). **Known undercount:** alias groups that span lines (`alias PramanaFoundry.DurableStore.{\n LegacyLine, …}`) and the kernel's `@families` short-name dispatch (`workflow/kernel.ex:86-102`) are missed; every "0 callers" row below was re-checked by an exact short-name grep before being called dead.

**Pins.** FR-08A (`lib/pramana_foundry/repair/fr08a_protected_boundary.ex:19-55`, report `docs/fr-08/fr08a-protected-report.txt`, rebind `bin/rebind_fr08a.exs`) pins 11 files: `durable_store/{authority,database,gateway,protected_primitives,kernel,record_codec,encoding,legacy_import,transition_plan,protected_verifier}.ex` and `repair/fr08_handoff_gate.ex`. H0 (`repair/h0_accepted_fr07_boundary.ex:32-50`, report `docs/fr-08/h0-accepted-fr07-report.txt`) pins `gateway, kernel, legacy_import, record_codec` and `git show`s the tags `pramana/af0c51b4…`, `pramana/a23482de…` (README:3-5; `foundry-ci.yml:22-24` sets `fetch-depth: 0` for them). FR-15aA (`ci/validate_fr15aa.exs:202-246`, digests checked at `:521`, manifest `docs/fr-15a/provisioning-manifest.exs`, test `test/pramana_foundry/repair/fr15aa_provisioning_test.exs:16`) sha256-pins `config/config.exs`, `config/schemas/assignment-v1.json`, `coordinator.ex`, `coordinator/tick.ex`, `agent_server.ex`, `herdr/{adapter,argv,runner}.ex`, `effects/{launch,prompt_delivery,process_group}.ex`, `checks/runner.ex`, `cli/rpc.ex`, `git_evidence.ex`, `docs/fr-09/pi_rpc_probe.exs`, `ci/toolchain.exs`; it was rebound on the branch at `cb64493` and `1ae3c3b` (`git log -- docs/fr-15a/provisioning-manifest.exs`). Its header still reads `schema: "pramana-foundry-fr15aa-provisioning/v1"`, `base_commit: f5067d96…` (a Pramāṇa SHA) and a macOS host profile with `/Users/raymondluong/.local/bin/omp`.

### 1.1 Subsystems

| Item (lib lines) | Verdict | Evidence: callers / tests | Knowledge to preserve → where | Pins | Ticket, size |
|---|---|---|---|---|---|
| `Coordinator`, `Coordinator.State`, `Coordinator.Tick` (2,287) | **delete** | lib callers: application (daemon mode only), agent_server, board, board/inspection, cli, hardening_pm, improver, system_metrics, transition. 14 test files name it: `coordinator_test` 863, `coordinator/engine_test` 243, `recovery_test` 147, `autonomous_launch_test` 1,221, `legacy_persistence_containment_test` 412, `fr05_containment_test` 413, `runtime_startup_boundary_test` 503, `daemon_recovery_test` 91, `stress_test` 468 (via `Transition`) | D6 "tick off by default, subscription gate refuses before admission" → already re-proved for the lane by `manual_lane/non_launch_test.exs:26` (module-closure check with two red controls). "PM proposals never act as admission" (O0 C9) → one clause in WORKFLOW-CONTRACT. `replace_projection` sender-without-handler defect (dead-surface inventory) dies with it | FR-15aA `coordinator.ex`, `tick.ex` | ML-DEL-DAEMON, L |
| `AgentServer` (941), `Effects.{Launch,PromptDelivery,SilenceWatchdog}` (296), `Herdr.{Adapter,Argv,Identity,Runner}` (617) | **delete** | lib callers of AgentServer: cli, coordinator, tick; Herdr.Adapter: agent_server, coordinator, tick, effects/launch, prompt_delivery. Tests: `agent_server_test` 893, `herdr/*` 548, `effects/{launch,prompt_delivery,silence_watchdog}_test` 408. `roles/*.md` are only read as prompt text at `agent_server.ex:648,670` | The OMP argv/identity binding (`Herdr.Argv`, `Identity`) is FR-09 material; STRATEGY says the harness is a substitution question (Pi vs Jido). Keep nothing; the FR-09 candidate is designed against `ExecutionBackend`, not Herdr | FR-15aA `agent_server.ex`, `herdr/*` ×3, `effects/launch.ex`, `effects/prompt_delivery.ex` | ML-DEL-DAEMON |
| `LaunchEligibility` (410), `Quota.{Quota,Cooldown,Fallback}` (261) | **operator question Q4** (delete now vs keep as pure-policy leaf) | callers: agent_server, coordinator, tick only. `autonomous_launch_test.exs:86-148` are pure policy tests (fail-closed for every role and profile state; no paid fallback; malformed policy → stable blocked reason); `:195-873` drive Tick/AgentServer and die with them. `quota/*_test` 194 | F01: "no paid fallback; explicit subscription eligibility; cooldown/unknown/restart" — the only executable record of the FR-01 policy shape. If deleted, copy the policy table from `launch_eligibility.ex:18-19` and the `:86-148` test names into the FR-09 design | none | ML-DEL-DAEMON (delete) or ML-POLICY-LEAF (S, keep) |
| Legacy JSONL persistence: `Transition` (851), `EventLog` (46), `Effects.Checkpoint` (52), `Import` (66), `ConsolidatedLog` (78), `LogStore` (37), `Schema` (487), `DurableStore.CompatibilityWriter` (48), `AtomicFile` (162) | **delete** | `Transition.rebuild` called from `coordinator.ex:161,294` and `board.ex:702`; `EventLog.append` from `coordinator.ex:1636`, `checkpoint.ex:27`; `Import.read` from `cli.ex:33,565`, `checkpoint.ex:34`. `AtomicFile` callers: checks/runner, legacy_import, telemetry/store — all legacy. Tests: `transition_test` 601, `stress_test` 468, `legacy_persistence_containment_test` 412, `checkpoint_test` 44, `schema_test` 175 (reads `test/fixtures/python/*`), `atomic_file_test` 52 | F02 "failed writes cannot acknowledge; torn tail preserved and rejected; corrupt history never resets" is already encoded for the SQLite store: `durable_store/sync_fault_test` 413, `operational_storage_test` 926, `fr08a_fr19a_integration_test` 652, `quarantine_exit_probe_test` 345. `EVENT_SOURCING.md` retires with it (DOC-RETIREMENT's OWNED-ELSEWHERE gate on FR-08B dissolves once the JSONL path is gone, not migrated) | none of these is FR-08A-pinned (`compatibility_writer.ex` is not in the list) | ML-DEL-DAEMON |
| `DurableStore.LegacyImport` (524), `LegacyLine` (28), `DurableStore.Kernel` behaviour (26) | **delete, separate ticket (pinned)** | `LegacyLine.classify` called at `authority.ex:736` and `legacy_import.ex:139` (multi-line alias; the matrix missed it). `DurableStore.Kernel` has no implementer (`rg '@behaviour'`, dead-surface inventory). `legacy_import_test` 242. Called in `lib/` only by the two attestation modules | Fresh stores (DOGFOOD-LOG Q1, decided) mean there is no JSONL to import. `authority.ex:736` must lose its legacy-line branch in the same change | FR-08A (`legacy_import.ex`, `kernel.ex`) → rebind; H0 (same two) → retire H0 (Q3) | ML-DEL-LEGACY-IMPORT, M; serial with any other pinned-file ticket |
| `Board.{Board,View,ViewState,Inspection,FindingsPanel}` (1,792), `Status.{Status,Report,TelemetryStatus}` (217), `Telemetry.{Telemetry,Store,Forecast,Observation}` (916), `Exports.TelemetryExport` (60), `SystemMetrics` (136) | **delete** | Board reads JSONL (`board.ex:702`) and targets node `pramana_foundry@hostname` (`board.ex:185-187`). Callers: cli, coordinator, improver, agent_server. Tests: `board_test` 475, `board/inspection_test` 111, `status/*` 76, `telemetry/*` 440, `system_metrics_test` 82 | F17 "unavailable/corrupt is not empty healthy" → `Observations` over the Gateway already exists (`observations_test` 1,269; FR-18A in progress). F22 redaction: the field list at `telemetry/telemetry.ex:216` (`prompt prompt_body response … credentials secrets provider_payload corpus_text`) and the assertion pattern `telemetry_test.exs:26-28,178` → carry into `ManualLane.Log` (`operator.log.jsonl`, `log.ex:176-182`, which today writes argv unredacted) | none | ML-DEL-DAEMON |
| `Improver` (994), `HardeningPM` (260), `PM.{PM,Proposal,AttemptCap}` (558), `Scheduler.{Scheduler,Policy}` (326) | **delete** | Started only by daemon mode (`application.ex:89-94`). `improver_proposal_gate_test` pins `propose: false` until FR-20; `hardening_pm_test`, `improver_test` pin the split-layout scope (batch 1). `scheduler/policy.ex:6` `@shared_resource_keys ~w(corpus database gpu other service_ports)` is Pramāṇa-shaped. Tests: `pm/*` 283, `scheduler_test` 277, `improver*` 49, `hardening_pm_test` 25 | Subcommit 3 removed PM from the kernel path (DOGFOOD-READINESS T2); `Workflow.Kernel.Software.Planning` holds the forward PM events. FR-12 admission/capacity and FR-20 constrained improvement are re-derived against Core; nothing here transfers. DOGFOOD-LOG **Q2 becomes moot** | none | ML-DEL-DAEMON |
| `Assignments.{Assignments,Handoff,Correction}` (264), `Reviews.{Reviews,Artifact,Matrix}` (161), `Integration.{Integration,Pipeline}` (76), `CLI.Validators` (289) | **delete** | Legacy handoff/review/integration ingress; callers coordinator/state, cli. `fr05_containment_test` 413 pins them; `assignments_test` 146, `reviews_test` 170 | FR-05 knowledge already re-encoded in the lane: candidate must be checkout HEAD with the base as ancestor (`git_evidence` refusal, RUNBOOK §5), receipt provenance (`receipt_provenance_mismatch`), reviewer independence (`principal_not_independent`, Core-enforced). **Gap:** `GitEvidence` (110 lines, used by `manual_lane/cli.ex`) has no direct test → ML-GITEVIDENCE-TEST | none | ML-DEL-DAEMON |
| `Cleanup` (536), `RuntimeOwner` (43), `RuntimeLease` (118), `Fence` (133) | **delete** | Daemon-mode only: lane mode is not wrapped in `RuntimeOwner` (`application.ex:44-49`). `Fence` callers: checks/runner, runtime_lease. Tests: `runtime_startup_boundary_test` 503, `daemon_recovery_test` 91, `fence_test` 18, `bin/test_daemon_recovery.sh` | F14 "two OS owners; stale PID is not authority; fail-closed takeover evidence" is re-implemented by `DurableStore.Owner` (189 lines) for the store; owner-lock behaviour is exercised in 10 `durable_store/*_test` files (`git grep -l -i owner -- test/pramana_foundry/durable_store`) and the lane's `gateway_recovery` route (RUNBOOK §5). The "clean release waits for the effect subtree to quiesce" test (`runtime_startup_boundary_test:53`) is FR-10 worker-monitor material → note in FR-10 design | none | ML-DEL-DAEMON |
| `Effects.ProcessGroup` (202, leaf, no `PramanaFoundry.*` deps), `Checks.{Runner,Status,Adoption}` (325) | **operator question Q5** | `ProcessGroup` callers: checks/* only; `Checks.Runner` has 0 lib callers (`git grep -w Runner -- lib` hits are `Herdr.Runner`). Tests: `process_group_test` 226 (`@moduletag :real_process`), `checks/*` 354 | F05 "foreign/recycled/unknown identities preserved; verified owned cleanup" lives in `ProcessGroup` + `docs/fr-04/identity-drift-probe.exs`; FR-10 worker monitoring will need exactly this. `Checks.*` is FR-13's check-receipt runner; the lane's check set is empty (RUNBOOK §7) | FR-15aA `process_group.ex`, `checks/runner.ex` | ML-KEEP-PROCESSGROUP (S: keep both as leaves, drop nothing) or fold into ML-DEL-DAEMON |
| `Assessor.*` (1,727, 13 files), `bin/assessor_eval.exs`, `docs/ASSESSOR.md` | **delete** (Q8 to confirm) | 0 lib callers outside `assessor/`; adapter chosen by config (`assessor.ex:20`), off by default; Jev HTTP fixture transport. Tests: `assessor/*` 957. Issue #26 is a Pramāṇa issue | Stage A research record → archive `docs/ASSESSOR.md` | none | ML-DEL-ASSESSOR, S (disjoint) |
| `Preparation` (18), `Parity` (30), `Projections.{Projection,Benchmark}` (351), `test/fixtures/{python,projections,telemetry}`, `config/schemas/*.json` | **delete** | Python-migration era; `docs/CI.md:83` says the parity harness was retired 2026-09-13. `policy_test` pins `Parity` as retired; `Preparation.commands` has no `lib/` caller (DOGFOOD-LOG Q4, ML-WORKFLOW-DIR); `benchmark_test` carries the `python_tiktoken_recompute` excluded tag. `config/schemas/*.json` (`$id: pramana-workflow/*`) are read by nothing in `lib/` (`git grep -n schemas -- lib` → empty); only `assignment-v1.json` is FR-15aA-pinned | Canonical encoding already lives in `DurableStore.Encoding.canonical`. **Q4 answered: delete** | FR-15aA `config/schemas/assignment-v1.json`; `ci.ex:43-45` `@excluded_tags` loses one tag | ML-DEL-PYTHON-ERA, S (after DAEMON removes the `cli.ex` `shadow`/`benchmark-projections` callers) |
| `Relocation` + `relocation/*` (2,340, 7 files) | **operator question Q6** (recommend retire) | Callers: none outside `relocation/`. Tests 1,000 lines in 9 files, 2 skipped "until FR-19B" (`relocation_test.exs:74`, `crash_recovery_test.exs:8`). `manifest.ex:172` special-cases `.pramana-supervisor`, `.pramana`, `pramana_tasks` | F21 "verify copy before removal; digest; live handles block; collision-safe rollback" → archive the design section of MIGRATION.md; `LocalExclude` "never sweep untracked `local/`" is moot once `local/` is gone. DOGFOOD-LOG **Q3 becomes moot** | none | ML-DEL-RELOCATION, M (disjoint) |
| `CLI` (630) + `CLI.RPC` (176) | **rewrite: lane-only** | `CLI.RPC.run` routes `["lane" \| argv]` to `ManualLane.CLI.main` and everything else to `CLI.main` (`rpc.ex:31-34`); shapes at `rpc.ex:127-160`. `cli.ex` has 0 `lane` references; its 30 `main/1` heads are legacy (`validate shadow runtime-root project-assignment telemetry-* benchmark-projections board health agents metrics logs handoff review ticket`). Tests: `cli_test` 364 (legacy), `rpc_wrapper_test` 279 + `cli/rpc_test` 165 (FR-02 envelope: keep) | FR-02 inert-transport containment (`bin/pramana` encoding, 64 KiB cap, duplicate-key/NUL rejection) is on the lane path → keep `CLI.RPC` and its two tests; the escript `main_module` (`mix.exs:10`) can point at `ManualLane.CLI` | FR-15aA `cli/rpc.ex` | ML-DEL-DAEMON |
| `Application` (125), `config/config.exs` | **rewrite** | daemon children `:68-95`; `PRAMANA_STARTUP_MODE` `:104`; `COORDINATOR_TICK` `:18`; config keys `herdr_command`, `herdr_timeout_ms`, `poll_seconds`, `max_assignments`, `max_tasks`; `startup_test.exs:97` pins the daemon child ids | Keep `runtime_children(:lane)` and `manual_lane_enabled?/0`; `startup_test.exs:46-66` covers them | FR-15aA `config/config.exs` (`legacy_not_provisioning`) | ML-DEL-DAEMON |
| `RuntimeRoot` (178) | **keep, rename env** | lane uses it (`manual_lane/server.ex`); reads `PRAMANA_RUNTIME_ROOT`, `PRAMANA_OPERATOR_RUNTIME_ROOT`, `PRAMANA_RUNTIME_ROOT_FRESH` | — | none | FR-23b env |
| `Repair.FR08AProtectedBoundary` (677), `Repair.FR08HandoffGate` (328) | **keep** | Boundary rule 5; `bin/rebind_fr08a.exs`; every Core change rebinds | — | self | — |
| `Repair.H0AcceptedFR07Boundary` (466) | **operator question Q3** (recommend retire) | Test reads two `pramana/<sha>` tags → CI `fetch-depth: 0`; "accepted FR-07 boundary" is superseded by FR-08A (REPAIR-PLAN:499-500) | The report is dated evidence → archive `docs/fr-08/h0-accepted-fr07-report.txt` | pins `legacy_import.ex`, blocking ML-DEL-LEGACY-IMPORT | ML-DEL-LEGACY-IMPORT |
| `DurableStore.*` forward set (`Authority, Capacity, Database, Encoding, Gateway, Maintenance, Owner, PathIdentity, ProtectedPrimitives, ProtectedVerifier, RecordCodec, TransitionPlan`) | **keep; decompose** | `Maintenance` is test-only (FR-19A); `Capacity` called from `gateway.ex`. Dead-surface inventory's FR-08B rows (`protected_schema_version/0`, `sidecars/1`, `reduce_projection_plan/2`, `discriminator_kinds/0`, 11 unused `@command_types`/`@intent_types` members) become removable under the fresh-store decision | The 4 recorded contract divergences (`93fb057`), R5 ledger Quint (`spec/ledger`), reopen property (`reopen_property_test`) | FR-08A, H0 | ML-DECOMPOSE-PP (L), ML-DECOMPOSE-GATEWAY (M), ML-DEAD-VOCAB (S) — after rename |
| `Workflow.Kernel*` (18 files) | **keep** | lane path; `@families` dispatch; evidence tools in `bin/` pin `lib/pramana_foundry/workflow/**` paths | — | none (rule 2: kernel never touches the store) | rename only |
| `ManualLane.*`, `WorkPacket`, `GitEvidence`, `Observations.*`, `SchemaReference`, `CI` | **keep** | forward; `ci.ex` carries names `pramana-foundry-ci-provenance/v2` (`ci.ex:9`), tmp prefix `pramana-foundry-ci-` (`ci.ex:59`), escript `pramana_foundry` (`ci.ex:385`), exclusion id `corpus-and-services` (`ci.ex:111-115`, asserted by `ci_test.exs:58`) | — | none | FR-23b |

Delete-list regex used for the 18,828-line count: `agent_server|assessor|assignments|board|checks|cli/validators|cleanup|consolidated_log|coordinator|effects/(checkpoint|launch|prompt_delivery|silence_watchdog)|event_log|exports|hardening_pm|herdr|import\.ex|improver|integration|launch_eligibility|log_store|parity|pm/|preparation|projections|quota|relocation|reviews|scheduler|schema\.ex|status|system_metrics|telemetry|transition\.ex|durable_store/(legacy_import|legacy_line|compatibility_writer|kernel)`. The complementary forward set (26,853 lines) is `manual_lane|work_packet|git_evidence|workflow/|durable_store/(the twelve above)|observations|runtime_root|runtime_owner|runtime_lease|fence|atomic_file|application|ci\.ex|cli\.ex|cli/rpc|repair/|schema_reference`; the four `runtime_owner|runtime_lease|fence|atomic_file` files (456 lines) are counted forward there but marked delete above.

### 1.2 Remaining old-layout and Pramāṇa assumptions in code

| Item | Evidence | Verdict |
|---|---|---|
| `foundry/` path prefix in non-doc files | `git grep -n -E '(^\|[^_a-z])foundry/' -- lib bin ci test config spec rel roles mix.exs` → 10 lines, all `/var/run/pramana-foundry/*.sock` in `ci/validate_fr15aa.exs:96-115` (spec strings, not paths). Batch 1 cleared the rest | done; the socket names go with FR-15aA (Q2) |
| `workflow/` prefix | 4 schema `$id`s (`config/schemas/*.json:3`), 3 fixture scopes (`test/fixtures/**:31,4-5`), `projection_test.exs:25`, `local_exclude_test.exs:20`, two moduledocs recording the old layout | die with §1 deletions |
| `automation/`, `bin/pramana-supervisor` | `roles/steerer.md:4,7,21,29`; `assignments_test.exs:14,53` | die with `roles/` and Assignments |
| Umbrella / Ecto / Postgres / corpus | `architecture_boundary_test.exs:53-54,123-137` (rule 6 guard: `@umbrella_apps [:pramana, :pramana_web, :pramana_native]`) — **keep**, it is the guard. `preparation.ex:8-11` `ecto.create` — delete. `roles/developer.md:81`, `roles/hardening_pm.md:10-17` — delete. `scheduler/policy.ex:6`, `schema.ex:465`, `telemetry.ex:216` `corpus` keys — delete. `mise.toml:2,8-10` comments about "corpus bake" and "dev repo SQL sandbox pool" — rewrite two comment lines | as listed |
| `Req`, `PramanaWorkflow` | 0 hits | none |
| `herdr` | 314 lines in 25 files (`lib bin ci test config spec rel roles`); `config.exs:17` `herdr_command: "/opt/homebrew/bin/herdr"` | dies with the Herdr adapter; `docs/README.md:68-76` "Provider and backend boundary" paragraph to rewrite |

## 2. Edge-case knowledge to keep

Everything in this table stays green after §1 and is not touched by any delete ticket unless the row says so.

| Mechanism | Where | Status under the sweep |
|---|---|---|
| Row/clause/from-cell coverage parsed from the contract at test time | `r4_coverage_test.exs` 2,735, `test/support/r4_rows.ex:20` (`docs/WORKFLOW-CONTRACT.md` path pinned), `kernel_walk.ex`, `kernel_search.ex` | keep; contract must not move (§3) |
| Reopen property (coverage floor, in the gate) | `durable_store/reopen_property_test.exs` 510; env `FOUNDRY_REOPEN_{RUNS,SEED,STATS,LIVE}` | keep |
| Restart probes | `{cancel,ledger,settle,set_control_cascade}_restart_probe_test.exs`, `manual_lane/restart_drill_test.exs` 577, `quarantine_exit_probe_test` | keep |
| Physical fault tests | `sync_fault_test` + `test/support/fr07_sync_fault.c` (compiled), `operational_storage_test` (3 darwin-only cases at `:489`), `fr08a_fr19a_integration_test` | keep; the two must not run concurrently (memory note) |
| Architecture boundary rules 1–6 with red controls | `architecture_boundary_test.exs`; `docs/BOUNDARY-RULES.md` (paths `lib/pramana_foundry/...` at `:3-5,19` change with the rename) | keep |
| Lane never launches (module closure, two red controls) | `manual_lane/non_launch_test.exs:26-42`; forbids `AgentServer, Coordinator*, Herdr.*` by name at `:18-21,65` | **rewrite with ML-DEL-DAEMON:** the forbidden list names modules that will no longer exist; keep the mechanism, retarget it at "no `System.cmd`/`Port`/launch effect module" |
| FR-02 inert RPC transport | `rpc_wrapper_test` 279, `cli/rpc_test` 165, `bin/pramana:88-127` | keep |
| Quint models | `spec/core_boundary/core.qnt` (18 invariants, `run.sh`), `spec/fr10/effects.qnt`, `spec/ledger/ledger.qnt` (cites `protected_primitives.ex` lines at Pramāṇa `df1ac5f8`/`33395c92`) | keep; `quint` is not installed locally (`which quint` → not found; `run.sh` uses `npx`); PP line citations drift with ML-DECOMPOSE-PP |
| Evidence tools | `bin/{refusal_sites,contract_annotation_diff,closure_probe,closure_cost,guard_mutation_sweep,coverage_guided_sweep}.exs`, `bin/preflight.sh` | keep; none is in the gate (BIN-SCRIPT-HEALTH); paths at `guard_mutation_sweep.exs:27,45-51`, `refusal_sites.exs:41-43`, `coverage_guided_sweep.exs:57-80` rename mechanically |
| FR-08A attestation + rebind | `repair/fr08a_protected_boundary.ex`, `bin/rebind_fr08a.exs`, `docs/fr-08/fr08a-protected-report.txt` | keep |
| Knowledge that moves (from deleted subsystems) | telemetry redaction fields → `ManualLane.Log`; FR-01 policy shape → FR-09 design (Q4); process-group identity drift → keep `ProcessGroup` (Q5); "clean release waits for effect subtree" → FR-10 design; F21 relocation rules → archived MIGRATION § | ML-KNOWLEDGE-NOTES, S: one dated note under `docs/design/` listing these five with their test names, written in the same batch as ML-DEL-DAEMON |

## 3. Documentation

DOC-RETIREMENT classified 183 `.md` at `e374322b`; there are 213 now. Its verdicts hold except where the mechanism is now scheduled for deletion rather than migration (the three OWNED-ELSEWHERE files and `ASSESSOR.md`), and its "no RETIRE before FR-08B" gate no longer applies.

**Files read by tests or tools at run time — cannot move without a rebind (8):**

| File | Reader |
|---|---|
| `docs/WORKFLOW-CONTRACT.md` | `test/support/r4_rows.ex:20`, `bin/contract_annotation_diff.exs`, `spec/ledger/ledger.qnt` |
| `docs/DURABLE-STORE-SCHEMA.md` | `test/pramana_foundry/schema_reference_test.exs` (generated; fails on drift) |
| `docs/fr-08/fr08a-protected-report.txt` | `repair/fr08a_protected_boundary_test.exs`, `bin/rebind_fr08a.exs` |
| `docs/fr-08/h0-accepted-fr07-report.txt` | `repair/h0_accepted_fr07_boundary_test.exs`, `durable_store/fr08a_fr19a_integration_test.exs` |
| `docs/fr-08/{fr08a-acceptance-review,fr08a-transition-review,fr08a-typed-replay-correction}-probes.exs` | `durable_store/fr08a_rereview_matrix_test.exs:6-50` (`Path.expand("../../..")`) |
| `docs/fr-15a/provisioning-manifest.exs` | `repair/fr15aa_provisioning_test.exs`, `ci/validate_fr15aa.exs` |
| `docs/fr-09/pi_rpc_probe.exs` | `ci/validate_fr15aa.exs:246` (`evidence_fixture` digest) |
| `docs/batch-d/lane-policy.example.json` | `manual_lane/startup_test.exs`, `bin/foundry-lane:20` |

Comment-only citations (safe to move, update the comment): `FR10-DESIGN` (`quarantine_exit_probe_test:2`), `fr08b-pure-kernel-review` and `COVERAGE-GUIDED-SWEEP` (`kernel_test:6,2567`), `THIN-LANE-DESIGN` (5 lane moduledocs), `fr-04/identity-drift-probe.exs` (`process_group.ex`), `docs/RULES.md` (`silence_watchdog.ex:47` — **does not exist**; dies with the file).

**Cited by no other file (0 inbound, `git grep -l -F <basename>` minus self):** `batch-d/DOGFOOD-LOG.md` (new; add to `docs/README.md`), `fr-01/review-v1.md`, `fr-02/review-v1.md`, `fr-04/current-process-state-review.md`, `fr-08/fr08a-acceptance-review.md`, `fr-08/fr08a-final-critical-review.md`, `fr-08/fr08a-independent-rereview.md`, `fr-08/fr08a-transition-replay-candidate.md`, `fr-08/fr08a-typed-carrier-candidate.md`. A further 40 have exactly one citer (mostly the README index or IMPLEMENTATION-LOG).

| Class | Files | Verdict | Ticket |
|---|---|---|---|
| **Current, keep at top level** | `README.md` (index; add DOGFOOD-LOG, drop the FR-08 review-chain rows once archived), `REPAIR-PLAN.md`, `WORKFLOW-CONTRACT.md`, `BOUNDARY-RULES.md`, `ELIXIR-CONVENTIONS.md`, `AGENT-BRIEF.md`, `EVIDENCE-TOOLS.md`, `CI.md`, `DURABLE-STORE.md`, `DURABLE-STORE-SCHEMA.md`, `STRATEGY.md`, `DOGFOOD-READINESS-2026-09-23.md`, `COVERAGE-GUIDED-SWEEP.md`, `conventions/phoenix/` | keep; rename paths | ML-DOCS-CURRENT, S |
| **Current design, group under `docs/design/`** | `ECOSYSTEM-BOUNDARY`, `ORCHESTRATOR-BOUNDARY`, `PROJECT-WORKFLOW-PROFILES`, `PLANNING-STRATEGIES`, `AX-SUBSTRATE`, `CLOUDFLARE-OS`, `PI-HARNESS`, `JIDO-HARNESS`, `orchestrator/O0,O1`, `fr-10/FR10-DESIGN`, `fr-08/{fr08b-kernel-correction-design, fr08b-row-driven-coverage, event-vocabulary-design, fr08b-event-vocabulary-enumeration, plan-binding-specification, plan-replay-revalidation-design, FR08B-SUBCOMMIT2-DECIDE-DESIGN, FR08B-SUBCOMMIT3-DESIGN, FR08B-PROTECTED-ITEMS-SPEC, FR08B-REVIEWER-INDEPENDENCE-DESIGN, FR08B-B3-GAP-INVENTORY, FR08B-B3-CONTRACT-READINGS-PROPOSAL, fr08b-ingress-inventory, fr08b-subcommit2-*-inventory, workflow-definition-seam}`, `fr-18a/bounded-effect-query-design` | move (comment citers only) | ML-DOCS-ARCHIVE, M |
| **Lane, group under `docs/lane/`** | `batch-d/*` (LANE-RUNBOOK, THIN-LANE-DESIGN, DOGFOOD-LOG, reviews/, lane-policy.example.json, thin-lane-review-findings) | move; rebind `startup_test` and `bin/foundry-lane:20` for the JSON | ML-DOCS-ARCHIVE |
| **Retire (mechanism deleted by §1)** | `EVENT_SOURCING.md` (JSONL), `MIGRATION.md` + `MIGRATION-TICKETS.md` (cutover/relocation; already bannered historical), `ASSESSOR.md`, `OBSERVABILITY.md` §inventory (legacy telemetry; rewrite around `Observations` + lane log), README §"Tracked layout" (`README.md:111-155` lists `agent_server.ex` first), §"CLI commands", §"Observability", §"Acceptance and activation containment" (`:33-46` names `pramana-live.sh` etc.) | retire/rewrite after ML-DEL-DAEMON | ML-DOCS-RETIRE, M |
| **Archive (dated evidence, `docs/archive/<ticket>/`)** | whole dirs `fr-01, fr-02, fr-03, fr-04, fr-05, fr-06, fr-07, fr-09 (keep pi_rpc_probe.exs in place while FR-15aA lives), fr-15a (same), fr-18a (minus the design), fr-19a, fr-21, audit-2026-09-12`; top-level `AUDIT-2026-09-12`, `ALIGNMENT-AUDIT-2026-09-19`, `alignment-disposition*`, `FR-06-DESIGN-REVIEW*`, `POSITIONING-AUDIT-2026-09-21`, `IMPLEMENTATION-LOG.md` (cited by 23 files, all prose links), `DOC-RETIREMENT-…`, `DEAD-SURFACE-…`, `BIN-SCRIPT-HEALTH-…`, `fr-23/*` (this file included, once acted on); `fr-08/` reviews, candidates, briefings, raw sweeps, probes (~55 files) except the 4 test-read files above | move; `bin/check_docs.exs` verifies links | ML-DOCS-ARCHIVE |
| **Pramāṇa links** | 25 links in 17 files to `lorecrafting/pramana/blob/e1e4b3bf/…`. In current docs: `AGENT-BRIEF.md:11` (worktree-bases rule → inline the sentence), `docs/README.md:58,87-98` (role contracts, catalog, repo map, shared workflow → drop), `ELIXIR-CONVENTIONS.md:14` (history, keep), `REPAIR-PLAN` FR-23 acceptance (dependency review runbook → inline or drop) | current docs stop routing to Pramāṇa | ML-DOCS-CURRENT |

Target tree: `docs/{README, STRATEGY, REPAIR-PLAN, WORKFLOW-CONTRACT, BOUNDARY-RULES, ELIXIR-CONVENTIONS, AGENT-BRIEF, EVIDENCE-TOOLS, CI, DURABLE-STORE, DURABLE-STORE-SCHEMA, OBSERVABILITY}.md`, `docs/lane/`, `docs/design/`, `docs/evidence/` (only the 8 pinned files, or leave them in place), `docs/conventions/`, `docs/archive/`. The cheap version (whole-directory moves, `fr-08` left in place) is what ML-DOCS-ARCHIVE should do; per-file `fr-08` triage is a second ticket.

## 4. Artifacts and repository hygiene

### 4.1 Ignored local data now in the checkout

| Path | Size / date | What | Worth keeping? |
|---|---|---|---|
| `local/state/current/` | 4.6 MB; `events.jsonl`, `coordinator.jsonl` + `.bak`, `.bak2`, `.pre-fr03-incident-repair-20260912` all dated 2026-09-12; `findings.jsonl` 3.4 MB **modified 2026-09-23 14:47** | legacy JSONL daemon state; `findings.jsonl` is written by `improver.ex:141`, so something ran the daemon stack on 2026-09-23 (**inferred**: the "stale legacy daemon" the handoff memory says was stopped) | no (fresh stores decided). Confirm `pgrep -fl 'sname pramana_foundry'` is empty, then delete |
| `local/artifacts/handoff-*.json` (5), `handoffs/` (8 files, 56 KB, 2026-09-12) | legacy handoff/review artifacts | no |
| `ci-artifacts/` | 2.4 MB; `provenance.json` for a gate at Pramāṇa `e1e4b3bf` (`source.commit`), plus the `pramana_foundry` escript | regenerated by every gate | no |
| `_build/` 13 MB, `deps/` 11 MB | ignored build state | fine |
| `erl_crash.dump` | `find . ~/dev/pramana -name erl_crash.dump` → none | — |
| `~/.local/state/foundry-lane/` | 4.5 MB; `state/manual-lane/authority.sqlite3` (+wal/shm), `operator.log.jsonl`, `authority.sqlite3.owner.sqlite3`, **`authority.sqlite3.owner.unclean`** | the live lane store. The `.owner.unclean` marker present means the store is fenced (`gateway_recovery`) until `lane recover` (**inferred** from the filename and RUNBOOK §5; not queried) | yes; archive, not delete, when the domain-tag rename lands (DOGFOOD-LOG Q1) |
| `~/dev/pramana/foundry/` | `ls` → No such file or directory (PR #51 removed it) | — | nothing left |
| `~/dev/pramana/.claude/worktrees/` | **40 agent worktrees** + 2 `pramana-supervisor` candidate worktrees + 1 `b5-probe` scratch (`git -C ~/dev/pramana worktree list`) — these are the WORKTREE-INVENTORY's pre-split rows | Pramāṇa's repository, not this one; the inventory's disposition rule still applies there | prune from `~/dev/pramana` (operator is on `chore/repo-sweep` there) |

### 4.2 Git state of `lorecrafting/foundry`

| Item | Evidence | Verdict |
|---|---|---|
| `.claude/worktrees/agent-{a52e366905e5a461d, a59e0d1f74d06878a, adf99f62d26cb7e1d}` at `3fece1a`, `05e2f3c`, `7a9b435` | all clean (`git status --short` → 0); `git cherry integ/lane-batch-1 <sha>` prints `-` for each → an equivalent patch is on main (`c8e0016`, `93fb057`, `1ae3c3b`+`8e61cd9`) | `git worktree remove` ×3, `git branch -D worktree-agent-*` ×3 |
| Local branches `chore/split-cleanup` (= `55037db`, PR #1 merged), `integ/lane-batch-1` (= main) | `git branch --format`, `gh pr list --state all` | delete local + remote |
| Remote tags `archive/2026-09-20/*` ×12 | pre-split branch tips, rewritten SHAs; IMPLEMENTATION-LOG:19-40 describes the convention | **operator question Q9** (history exists in Pramāṇa; keeping them costs nothing but clutters `git tag`) |
| Remote tags `pramana/<sha>` ×2 | read by the H0 test; force `fetch-depth: 0` in CI | delete with H0 (Q3) |
| `refs/pull/1,2` | GitHub-managed | — |

### 4.3 Workflows, toolchain, release

| Item | Evidence | Verdict |
|---|---|---|
| `.github/workflows/foundry-ci.yml` | runs `bin/check_docs.exs` + `ci/run.exs`; `fetch-depth: 0` comment names the tags | keep; drop `fetch-depth` with H0 |
| `.github/workflows/fr19a-sync-eio.yml` (262 lines) + `ci/fr19a_*` (5 files, 1,281 lines) + `test/fr19a_*` (2) + `test/support/fr19a_*` (2) | `if: github.ref == 'refs/heads/repair/fr19a-correction'` (`:32`); that branch is absent from `git ls-remote origin` (only `main`, `chore/split-cleanup`, `integ/lane-batch-1`); paths still say `lib/pramana_foundry/durable_store/**`; `ci_test.exs:59` asserts only on `foundry-ci.yml` | **operator question Q7**: archive scripts under `docs/archive/fr-19a/` as FR-19A evidence and delete workflow + tests (recommended), or keep the workflow retargeted at `main` |
| `mise.toml` | pins erlang 29.0.5 / elixir 1.20.3-otp-29; comments `:2` "corpus bake", `:8-10` "dev repo … SQL sandbox pool" are Pramāṇa's | rewrite two comments |
| `rel/vm.args` vs `rel/overlays/vm.args` | `diff` → identical; `mix.exs:11` uses `vm_args: "rel/vm.args"` | delete the overlay copy; `rel/overlays/env.sh:19` comment `RELEASE_NODE=pramana_workflow` |
| `mix.exs` | app `:pramana_foundry`, release `pramana_foundry`, escript `PramanaFoundry.CLI` | FR-23b |
| `.gitignore` | `/pramana_foundry` (escript), `/local/`, `/handoffs/`, `/.claude/` | rename escript line; drop `/local/`, `/handoffs/` after 4.1 |
| `bin/` | `foundry-lane` (keep; `PRAMANA_RELEASE`, `PRAMANA_RUNTIME_ROOT` at `:16-18`), `pramana` (rewrite lane-only; header `:11-16` documents legacy commands; `$HOME/foundry` fallbacks `:62-64`), `check_docs.exs`, `rebind_fr08a.exs`, `sync_phoenix_rules.exs`, the six evidence tools, `preflight.sh`/`freeze-evidence.sh` (keep, per BIN-SCRIPT-HEALTH), `test_daemon_recovery.sh` (delete with `daemon_recovery_test`), `live_test.exs`/`pramana-live.sh`/`tickets_from_review.sh` (FR-05 disabled stubs; `pramana-live.sh` is pinned by `fr05_containment_test` → delete together), `assessor_eval.exs` (with Assessor) | as listed |
| `spec/` | keep all three models; `spec/core_boundary/unbound_nonstart_probe.exs` referenced by its README only | keep |
| `roles/` (5 files, 288 lines) | read only by `agent_server.ex:648,670`, `hardening_pm.ex:9`, `agent_server_test.exs:220`; `steerer.md` drives a supervisor that does not exist (`:4,21,29`) | delete all five with ML-DEL-DAEMON (retires steerer as planned); the developer/reviewer instructions already live in LANE-RUNBOOK §3 and the packet |

### 4.4 Operator memory (`~/.claude/projects/-Users-raymondluong-dev-foundry/memory/`, 21 files) — stale pointers only, not edited

| File | Stale pointer |
|---|---|
| `foundry-test-suite-gotchas.md:8,130` | `~/dev/pramana/foundry`, `foundry/docs/REPAIR-PLAN.md`; "nine protected modules" (FR-08A pins 11 files today) |
| `foundry-goal-dogfood-alpha.md:11-16` | "in `~/dev/pramana`", `foundry/docs/…`; "finish FR-08B, then Batch C, then Batch D" predates the M1–M5 amendment |
| `pramana-steerer-role.md` | already marked superseded at `:11`; body still points at `foundry/roles/steerer.md` in `dev/pramana` |
| `never-commit-directly-to-integration-checkout.md` | legacy supervisor `state.json`/`accepted_revision`, `automation/roles/steerer.md` — no such mechanism in this repo |
| `parallel-sessions-sweep-uncommitted-files.md:11` | "two sessions against `~/dev/pramana`" |
| `measurements-must-ship-with-the-claim.md:35`, `generative-tests-beat-point-reviews.md:12`, `delete-duplication-before-checking-it.md:15`, `review-findings-must-land-in-the-repo.md:16,21` | `foundry/docs/…`, `foundry/roles/steerer.md` paths |
| `handoff-2026-09-23-fr08b.md` | pre-split state (`repair/fr08b-kernel`, `e1e4b3bf`); its "next session order" is done through step (1); keep as history or fold into a post-split handoff |
| `worktree-agents-may-branch-from-main.md`, `claims-go-stale-not-code.md` | `repair/fr08b-kernel` (a Pramāṇa branch) |
| `foundry-gate-provenance-keys.md` | schema `pramana-foundry-ci-provenance/v2` — true today; changes with FR-23b |
| `MEMORY.md:1,6` | already routes the split and the steerer; fine |

## 5. Names: the `pramana` surface beyond the planned renames

| Surface | Evidence | Disposition |
|---|---|---|
| Node names | `board.ex:185-187` `pramana_foundry@hostname` (legacy daemon); lane uses `RELEASE_NODE=foundry_lane` (`bin/foundry-lane:17`); `rel/overlays/env.sh:19` comment `pramana_workflow` | dies with Board; fix the comment |
| Agent principal names | `agent_server.ex` `"pramana-dev-"`, `"pramana-review-"`; test fixtures `pramana-dev-runtest` ×13, `pramana-review-runtest` ×4 | die with AgentServer; lane convention is `agent:<model>/dev-<ticket>` (RUNBOOK §4) |
| Tmp / run-root prefixes | `pramana-foundry-ci-` (`ci.ex`), `pramana-foundry-test-operator` (`config.exs:6`), `pramana-foundry-mix-test-`, `pramana-build-`, `pramana_cli_test_`, `pramana-relocation-`, `pramana-fr08a-gate-`, `pramana-dirty-checkout-` (tests) | FR-23b env/rename ticket; grep list: `git grep -h -o -E 'pramana[-_][a-z0-9_-]+' -- lib bin ci test config rel mix.exs \| sort \| uniq -c` (63 distinct) |
| Digest domain tags (persisted, checked on read; `record_codec.ex:258` per DOGFOOD-LOG Q1) | 11 distinct `pramana-foundry-*-v1/v2` in `protected_primitives.ex`, `gateway.ex`, `record_codec.ex`; `pramana-foundry-h0-*`, `-fr08a-*` in `repair/` | planned hard rename, fresh stores |
| Schema / provenance ids | `pramana-workflow/*` (`config/schemas`), `pramana-foundry-ci-provenance/v2` (`ci.ex`), `pramana-foundry-fr15aa-provisioning/v1` (manifest) | first dies with §1; others FR-23b |
| Telemetry/status keys | `status/report.ex` `:pramana_runtime_implementation_revision` | dies with Status |
| Relocation manifest entries | `manifest.ex:172` `.pramana-supervisor`, `.pramana`, `pramana_tasks` | die with Relocation (Q6) |
| Rule-6 guard vocabulary | `architecture_boundary_test.exs:53-54` `:pramana, :pramana_web, :pramana_native` | **keep**: it is the guard that Foundry never depends on Pramāṇa |
| Socket paths | `/var/run/pramana-foundry/*.sock` ×7 in `validate_fr15aa.exs:96-115` | with FR-15aA (Q2) |
| Prose | 1,998 lines in docs, 22 in README, 17 in roles, 1 in AGENTS.md ("Pramāṇa's corpus", correct) | archive/rewrite per §3; dated records are not rewritten |

## 6. Proposed lane ticket sequence

Each batch's file sets are disjoint so its tickets can run in parallel worktrees; one serial gate per batch (memory: parallel-worktree-agents). "Deleting a subsystem before renaming it saves renaming it" is the ordering principle: Batch A removes ~41% of `lib/` and most non-doc `pramana` hits before FR-23b touches anything.

| Batch | Ticket | Files (disjoint within the batch) | Size | Must precede / follow |
|---|---|---|---|---|
| **A0 (operator, no agent)** | hygiene: rm `local/ handoffs/ ci-artifacts/`; remove 3 worktrees + branches; delete merged branches local+remote; run `lane recover` if the store is fenced; answer Q1–Q10 | none tracked | S | before A1 |
| **A1** | **ML-DEL-DAEMON**: §1 delete rows (Coordinator*, AgentServer, Herdr, launch Effects, JSONL persistence, Board/Status/Telemetry/Exports/SystemMetrics, Improver/HardeningPM/PM/Scheduler, Assignments/Reviews/Integration/CLI.Validators, Cleanup/RuntimeOwner/RuntimeLease/Fence/AtomicFile, `cli.ex` → lane-only, `Application`/`config.exs` lane-only, `roles/`, 4 bin stubs, ~59 test files, `non_launch_test` retarget) + FR-15aA disposition per Q2 (rebind to the surviving pins or archive validator+manifest+test) | one connected component; cannot be split across agents (every caller edge above runs through `coordinator.ex`/`cli.ex`) | L | first; before every rename |
| A1 parallel | **ML-DEL-ASSESSOR** (`lib/pramana_foundry/assessor/`, `test/…/assessor/`, `bin/assessor_eval.exs`, `docs/ASSESSOR.md`, `docs/README.md` row) | disjoint | S | with A1 |
| A1 parallel | **ML-DEL-RELOCATION** (Q6) (`relocation.ex`, `relocation/`, 9 tests, `docs/README.md` row) | disjoint | M | with A1 |
| A1 parallel | **ML-DEL-FR19A-EIO** (Q7) (`fr19a-sync-eio.yml`, `ci/fr19a_*`, `test/fr19a_*`, `test/support/fr19a_*`, `docs/fr-19a/linux-sync-eio-plan.md` note) | disjoint | S | with A1 |
| A1 parallel | **ML-KNOWLEDGE-NOTES**: the five moved-knowledge items of §2 as one dated design note; redaction test for `ManualLane.Log` | new doc + `manual_lane/log.ex` + its test | S | with A1 (log.ex is not in A1's set) |
| **A2** (after A1 gate) | **ML-DEL-PYTHON-ERA** (`preparation.ex`, `parity.ex`, `projections/`, `schema.ex` if A1 left it, `test/fixtures/{python,projections,telemetry}`, `config/schemas/`, `policy_test`, `preparation_test`, `projections/*_test`, `ci.ex` `@excluded_tags`) | needs A1's `cli.ex` rewrite | S | after A1 |
| A2 parallel | **ML-DEL-LEGACY-IMPORT** (Q3): `legacy_import.ex`, `legacy_line.ex`, `durable_store/kernel.ex`, `authority.ex:736` branch, `legacy_import_test`, H0 module/test/report, 2 tags, `fetch-depth`; **one FR-08A rebind** | touches pinned files → serial with any other Core edit | M | after A1; before FR-23b (the rename would otherwise rebind these twice) |
| A2 parallel | **ML-DOCS-ARCHIVE** (whole-dir moves, `docs/lane/`, `docs/design/`, `bin/check_docs.exs` green) and **ML-DOCS-RETIRE** (EVENT_SOURCING, MIGRATION ×2, OBSERVABILITY rewrite, README sections) | docs only; RETIRE depends on A1's deletions being real | M + M | after A1 |
| A2 parallel | **ML-GITEVIDENCE-TEST**, **ML-LANE-QUIET-RPC** (F1), **ML-LANE-INTEGRATE** (F6), **ML-LANE-NOTES-ARCHIVE** (F5), reviewer-worktree runbook line (F4) | `git_evidence.ex` test; `manual_lane/{cli,server,backend}.ex` (one owner) | S each | any time; F1/F5/F6 share `manual_lane/cli.ex` → one agent |
| **B (FR-23b renames, after A2 gate)** | **ML-RENAME-NS** (`PramanaFoundry`→`Foundry`, `:pramana_foundry`→`:foundry`, `lib/pramana_foundry/`→`lib/foundry/`, `test/pramana_foundry/`→`test/foundry/`, release/escript names, `.gitignore`, evidence-tool paths, BOUNDARY-RULES paths, `ci.ex` schema/tmp names) + one `rebind_fr08a.exs` + FR-15aA rebind if it survived Q2 | everything; one mechanical commit when nothing else is in flight | L | after A2 |
| B serial | **ML-RENAME-BIN-ENV** (`bin/pramana`→`bin/foundry` lane-only; `PRAMANA_*`→`FOUNDRY_*` in `runtime_root.ex`, `application.ex`, `config.exs`, `ci.ex`, `bin/foundry-lane`, RUNBOOK table; `rel/overlays` cleanup; `mise.toml` comments) | bin/config/rel + the few lib readers | S | after ML-RENAME-NS (paths) |
| B serial | **ML-RENAME-DOMAIN-TAGS** (11 tags + `repair/` tags; archive the lane store; fresh store; FR-08A rebind) | `protected_primitives.ex`, `gateway.ex`, `record_codec.ex`, `repair/*` | M | last in B (pinned files; one rebind) |
| **C (post-rename)** | **ML-DECOMPOSE-PP** (L), **ML-DECOMPOSE-GATEWAY** (M), **ML-DEAD-VOCAB** (dead-surface FR-08B rows, now unblocked; S), **ML-GATE-EVIDENCE-TOOLS** (`refusal_sites`, `contract_annotation_diff` as ExUnit; S), `docs/fr-08` per-file triage (M), Quint line-citation refresh (S) | Core files; serial by rebind | — | after B |

**Plan consequence (Q1).** REPAIR-PLAN:521 makes FR-23b depend on FR-08B, FR-12 and FR-19B, and FR-08B subcommit 5 is "the four ingress migrations that retire the legacy JSONL paths" (DOGFOOD-READINESS §2 D2). Batch A deletes those ingresses instead of migrating them, so subcommit 5's public-ingress slice is already the lane and the other three slices have no subject; FR-10/11/12's "files it rewrites" (`FR-23-SPLIT…:118-124`: `coordinator.ex`, `tick.ex`, `scheduler/`, …) no longer exist. This needs a plan amendment before A1, not after.

## 7. Operator questions

| # | Question | Recommendation | Blocks |
|---|---|---|---|
| Q1 | Amend REPAIR-PLAN: FR-23b's dependency on FR-12/FR-19B and FR-08B subcommit 5's four migrations are replaced by "delete the legacy ingress; the lane is the ingress"; FR-10/11/12 are rebuilt on Core, not rewritten from the Coordinator | yes, one dated amendment like the M1–M5 one | A1 |
| Q2 | FR-15aA evidence: 12 of its 16 repository pins are files A1 deletes. (a) Archive manifest + validator + test as dated FR-15aA evidence and drop the test from the gate; (b) re-pin to the surviving files (`cli/rpc.ex`, `git_evidence.ex`, `process_group.ex`?, `ci/toolchain.exs`) with a "no executable route" inventory | (a); the ticket is Complete and its subject (the OMP route) is being removed | A1 |
| Q3 | Retire H0 (`H0AcceptedFR07Boundary`, test, report, two `pramana/<sha>` tags, CI `fetch-depth: 0`)? | yes; FR-08A supersedes it (REPAIR-PLAN:499-500) | ML-DEL-LEGACY-IMPORT |
| Q4 | `LaunchEligibility` + `Quota`: delete with the daemon, or keep as pure-policy leaves with `autonomous_launch_test:86-148` and `quota/*_test`? | keep as leaves (671 lines, no deps) until the FR-09 design says what replaces them | A1 scope |
| Q5 | `Effects.ProcessGroup` + `Checks.*`: keep as leaves for FR-10/FR-13? | keep `ProcessGroup` (+ its test); delete `Checks.*` (FR-13 will re-derive check receipts from the kernel's `check_*` events) | A1 scope |
| Q6 | Relocation (FR-19B "repair or retire"): retire now? | retire; archive the F21 rules | ML-DEL-RELOCATION |
| Q7 | FR-19A sync-EIO workflow + scripts + tests: archive as evidence or keep the workflow retargeted at `main`? | archive; the workflow has never been able to run on this remote | ML-DEL-FR19A-EIO |
| Q8 | Delete the Assessor (issue #26 Stage A)? | yes | ML-DEL-ASSESSOR |
| Q9 | Keep the 12 `archive/2026-09-20/*` tags? | delete; the history is in Pramāṇa and the tags carry rewritten SHAs nobody cites | A0 |
| Q10 | Docs: cheap archive (whole dirs, `fr-08` untouched) now, or full per-file triage in one ticket? | cheap now; `fr-08` triage in Batch C | ML-DOCS-ARCHIVE |
| Q11 | Was a legacy daemon running on 2026-09-23 (`local/state/current/findings.jsonl` written 14:47)? Confirm none runs before A1 | `pgrep -fl 'sname pramana_foundry'` | A0 |
| Q12 | Confirm DOGFOOD-LOG Q2 (HardeningPM scope) and Q3 (LocalExclude anchor) are closed as moot by A1/ML-DEL-RELOCATION | yes | — |

## 8. Commands run (for reproduction)

`git log --oneline -15 integ/lane-batch-1`; `git status --short`; `git worktree list`; `git ls-remote origin`; `git branch --format`; `git cherry integ/lane-batch-1 <sha>`; `gh pr list --state all --json …`; `gh pr checks 2`; `gh run list --limit 6`; `git ls-files | cut -d/ -f1 | sort | uniq -c`; `git ls-files <dir> | xargs wc -l`; `git ls-files -z | xargs -0 du -k | sort -rn`; `git grep -i -c pramana integ/lane-batch-1 -- .`; `git grep -h -o -E 'PRAMANA_[A-Z_]+' …`; `git grep -h -o -E 'pramana[-_][a-z0-9_-]+' -- lib bin ci test config rel mix.exs`; `git grep -n -E '(^|[^_a-z])foundry/' -- lib bin ci test config spec rel roles mix.exs`; per-module caller scan (`git grep -l -P` as described in §1); exact short-name re-checks (`git grep -n -w -E <Name> -- lib`); `git grep -n -o -E 'docs/[A-Za-z0-9_./-]+' -- lib test ci bin spec`; per-doc inbound count (`git grep -l -F <basename> -- .` minus self); `git grep -c 'lorecrafting/pramana/blob/e1e4b3bf' -- .`; `git log --oneline -- docs/fr-15a/provisioning-manifest.exs ci/validate_fr15aa.exs`; `diff rel/vm.args rel/overlays/vm.args`; `find . ~/dev/pramana -name erl_crash.dump`; `ls -la local/state/current handoffs`; `du -sh local handoffs ci-artifacts _build deps ~/.local/state/foundry-lane`; `find ~/.local/state/foundry-lane -maxdepth 3`; `ls ~/dev/pramana/foundry`; `git -C ~/dev/pramana worktree list`; `which quint`; `grep -n` over `~/.claude/projects/-Users-raymondluong-dev-foundry/memory/*.md`. Files read in full: `application.ex`, `config/config.exs`, `mix.exs`, `mise.toml`, `.gitignore`, `bin/pramana`, `bin/foundry-lane`, `rel/*`, both workflows, `docs/README.md`, `AGENTS.md`, `README.md:1-120`, DOGFOOD-READINESS, LANE-RUNBOOK, DOGFOOD-LOG, AGENT-BRIEF, BOUNDARY-RULES, STRATEGY §working summary, REPAIR-PLAN §delivery strategy, §FR-23, ticket table, the five prior inventories, `preparation.ex`, `policy_test.exs`, `test_helper.exs`, and the grep-selected lines cited above.
